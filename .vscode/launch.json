{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/map-app-react/package.json",
                "localRoot": "${workspaceFolder}/map-app-react"
            }
        }
    ]
}